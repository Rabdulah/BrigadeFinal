<div class="as-calendar-occurrence__container">

  {{#if isBooked}}
    {{#if hasBlock}}
      {{yield}}
    {{else}}
      <h1 class="as-calendar-occurrence__title" style={{titleStyle}}>{{title}}</h1>
      <h1> {{startingTime}} - {{dayEndingTime}}</h1>
      <h1>testing true isBooked</h1>
    {{/if}}
  {{else}}
    {{#if hasBlock}}
      {{yield}}
    {{else}}
      <h1 class="as-calendar-occurrence__title" style={{titleStyle}}>{{title}}</h1>
      <h1> {{startingTime}} - {{dayEndingTime}}</h1>
    {{/if}}
  {{/if}}

</div>
